# suppress inspection "UnusedProperty" for whole file


#------------------------------------------------------------------------------#
# Databases
#------------------------------------------------------------------------------#

# Test database

authserver.datasource.test.driver-class=org.h2.Driver
authserver.datasource.test.jdbc-url=jdbc:h2:file:~/test
authserver.datasource.test.username=sa
authserver.datasource.test.password=
spring.jpa.database=h2
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.H2Dialect

# H2 Console
spring.h2.console.enabled=true


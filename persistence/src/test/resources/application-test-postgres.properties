# suppress inspection "UnusedProperty" for whole file

#------------------------------------------------------------------------------#
# Databases
#------------------------------------------------------------------------------#

# Main database
authserver.datasource.test.driver-class=org.posgresql.Driver
authserver.datasource.test.jdbc-url=jdbc:postgresql://localhost:5432/test
authserver.datasource.test.username=test
authserver.datasource.test.password=test
spring.jpa.database=mysql
# Allows Hibernate to generate SQL optimized for a particFox-testr DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQL9Dialect

# H2 Console
spring.h2.console.enabled=false


spring.jpa.hibernate.ddl-auto=update
